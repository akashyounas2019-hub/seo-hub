version: '3.8'

services:
  seo-hub:
    build: .
    container_name: seo-hub-app
    restart: always
    ports:
      - "3030:3030"
    environment:
      - PORT=3030
      - HOST=0.0.0.0
      - NODE_ENV=production
      - ADMIN_EMAIL=admin@example.com
      - ADMIN_PASSWORD=changeme123
      - GYL_WORKER_SECRET=default_secret
